# recon-bash
Recon script based on bash by using tools (ping, nslookup, nmap)

## Usage:
    ```
    chmod +x ./recon.sh
    ```
    
    ```
    ./recon.sh
    ```

    Script only for education and fun usage.
